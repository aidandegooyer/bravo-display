[project]
name = "bravo-display"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.1.1",
    "websockets>=16.0",
]

[project.optional-dependencies]
simconnect = [
    "simconnect>=0.4.26",
]
